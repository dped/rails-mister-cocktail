<h1><%= @cocktail.name %></h1>

<% @cocktail.doses.each do |dose| %>
<h5><%= dose.ingredient.name %> <%= dose.description %>     <%= link_to "Delete", dose_path(dose),
    method: :delete,
    data: { confirm: "Are you sure?"} %></h5>



<% end %>

<h5><%= link_to "Add a new dose", new_cocktail_dose_path(@cocktail) %></h5>
